import CoreFeatures from "@/components/CoreFeatures";

// Standalone preview of the Core Features section (light theme).
export default function PreviewPage() {
  return (
    <div className="min-h-screen bg-white">
      <CoreFeatures />
    </div>
  );
}
