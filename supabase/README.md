# Supabase migrations

Run these in order in the Supabase SQL editor. Naming: `NNN_name.sql`.

- `001_init.sql` — schema (roles, candidates, transcripts, verdicts) + `recordings`
  storage bucket and permissive demo policies.

Future SQL goes here as `002_…sql`, `003_…sql`, etc.
